import { SmoothFadeDirective } from './smooth-fade.directive';

/*describe('SmoothFadeDirective', () => {
  it('should create an instance', () => {
    const directive = new SmoothFadeDirective();
    expect(directive).toBeTruthy();
  });
});
*/
